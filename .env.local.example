# ── Firebase (client — exposed to browser) ───────────────────────────────────
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# ── Firebase Admin (server-only — NEVER expose to client) ────────────────────
# Download the service-account JSON from Firebase Console → Project Settings → Service accounts
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
# Paste the private key as-is (quotes included); Next.js handles the escaping
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# ── Data source ───────────────────────────────────────────────────────────────
# "mock" uses in-memory fixtures; "firebase" reads live Firestore data
NEXT_PUBLIC_DATA_SOURCE=mock

# ── Prometheus external sync (server-only — NEVER expose to client) ───────────
# Credentials for the external data source at gcsar.gov.sy
PROMETHEUS_URL=http://gcsar.gov.sy/ar/prometheus
PROMETHEUS_USERNAME=info-dept
PROMETHEUS_PASSWORD=

# ── App ───────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_DEFAULT_LOCALE=ar
